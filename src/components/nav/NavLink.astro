---
const { title, href, selected, target } = Astro.props;
---

<a href={href} class={selected ? "selected" : ""} target={target}>{title}</a>

<style>
  a {
    text-decoration: none;
    font-weight: bold;
    color: white;
  }

  a:hover {
    color: #3c3951;
  }

  a.selected {
    text-decoration: underline;
    text-decoration-thickness: 3px;
  }
</style>
